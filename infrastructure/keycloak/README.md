# Keycloak infrastructure

Phase 1 will add reproducible local realm configuration here. Realm exports may
contain clients, roles, scopes, and safe development users, but must not become
a store for production secrets.

