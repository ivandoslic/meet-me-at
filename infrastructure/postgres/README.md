# PostgreSQL infrastructure

One PostgreSQL container may host several local databases, but each service must
use a distinct database or schema and database principal. Service migrations
remain owned by the service rather than this directory.

