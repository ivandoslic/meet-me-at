# Infrastructure

Local platform components are configured here and orchestrated from the root
Compose file.

Planned areas:

- `keycloak/` identity configuration and development theme
- `postgres/` local database initialization boundaries
- `rabbitmq/` broker definitions
- `minio/` object-storage configuration
- `observability/` telemetry collection and visualization

Generated data volumes do not belong in version control.

