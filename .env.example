# Safe Phase 0 defaults only. Add component-specific values with the milestone
# that introduces that component. Never place real secrets in this file.
COMPOSE_PROJECT_NAME=meet-me-at
TZ=Europe/Zagreb

