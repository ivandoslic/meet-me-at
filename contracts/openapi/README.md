# OpenAPI contracts

Each externally consumed service API will have a versioned OpenAPI description.
The API gateway exposes the public surface, while internal endpoints remain
clearly identified.

No API exists in Phase 0.

