# Web application

Reserved for the React and TypeScript web client.

Planned authentication: OpenID Connect Authorization Code Flow with PKCE as a
public client. The web client calls the API gateway and does not call internal
services directly.

