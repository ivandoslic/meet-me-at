# Future iOS application

Reserved for a future iOS client. iOS implementation is deferred because local
iOS compilation and simulation require macOS and Xcode.

The default future choice is a native SwiftUI client. Kotlin Multiplatform may
later be evaluated for sharing non-UI code, but it does not remove Apple's build
toolchain requirement.

