# Android application

Reserved for the Kotlin and Jetpack Compose Android client.

The application runs on a device or Android Emulator, not in Docker. It uses the
system browser for OpenID Connect login and calls the API gateway with access
tokens.

